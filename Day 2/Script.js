const task = ['coding', 'eating', 'sleeping', 'playing', 'reading'];
let i = 0;

while (i < task.length) {
    console.log(task[i]);
    console.log(`${i + 1}. ${task[i]}`)
    i++;
}
// this is a while loop that iterates through the task array and logs each task to the console until it reaches the end of the array.

const name = [" mildred", " vanessa", "samatha", "jessica", "ida", "favour", "Joy"];
let all = 0;

while (all < name.length){
    console.log(`${all + 1}. ${name[all]}`);
    all++;
}

const favouriteFood = ["achu", "cornchaff", "jollof rice", "fried rice", "bread and beans", "yam", "plantain"];
let myFood = 0;

while (myFood < favouriteFood.length){
    console.log(`${myFood + 1}. ${favouriteFood[myFood]}`);
    myFood++;
};